## Run script.py to download all raw variables available in database:
`python script.py` 

## Run covariables.py to download a subset of covariables:
`python covariables.py --all-variables`

### Data to upload must be forest_data.csv for both otherwise use available command line arguments to change the name of the input file
